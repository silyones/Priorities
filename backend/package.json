{
  "name": "peoples-priorities-backend",
  "version": "1.0.0",
  "description": "People's Priorities — FastAPI backend",
  "scripts": {
    "predev": "python scripts/kill_port.py 3001",
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 3001",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 3001"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "firebase": "^12.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
